// src/core/importXml.js

const fs = require("fs");
const path = require("path");
const xml2js = require("xml2js");

// Funzione principale
async function importXml(xmlPath, folderPath) {
  // 1. leggere XML
  // 2. parsarlo
  // 3. convertirlo nel modello canonico
  // 4. aggiungere info locali
  // 5. salvare JSON
}

module.exports = { importXml };
